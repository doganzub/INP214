from setuptools import setup, find_packages

setup(
    name='google-colab-compat',
    version='1.0.0',
    description='Local compatibility layer for Google Colab notebooks',
    packages=find_packages(),
    python_requires='>=3.6',
)
